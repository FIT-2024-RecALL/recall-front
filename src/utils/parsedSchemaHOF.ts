import { FieldValues } from 'react-hook-form';
import { ZodAny, ZodObject, ZodParsedType } from 'zod';

// export const parsedHOF =
//   (schema: ZodObject, f: (data: ZodParsedType) => void) => (data: FieldValues) => {
//     f(schema.parse(data));
//   };
